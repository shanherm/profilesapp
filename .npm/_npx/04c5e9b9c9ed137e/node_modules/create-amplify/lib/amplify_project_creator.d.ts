import { PackageManagerController } from '@aws-amplify/plugin-types';
import { ProjectRootValidator } from './project_root_validator.js';
import { GitIgnoreInitializer } from './gitignore_initializer.js';
import { InitialProjectFileGenerator } from './initial_project_file_generator.js';
/**
 * Orchestration class that sets up a new Amplify project
 */
export declare class AmplifyProjectCreator {
    private readonly projectRoot;
    private readonly packageManagerController;
    private readonly projectRootValidator;
    private readonly gitIgnoreInitializer;
    private readonly initialProjectFileGenerator;
    private readonly defaultDevPackages;
    private readonly defaultProdPackages;
    /**
     * Orchestrator for the create-amplify workflow.
     * Delegates out to other classes that handle parts of the getting started experience
     */
    constructor(projectRoot: string, packageManagerController: PackageManagerController, projectRootValidator: ProjectRootValidator, gitIgnoreInitializer: GitIgnoreInitializer, initialProjectFileGenerator: InitialProjectFileGenerator);
    /**
     * Executes the create-amplify workflow
     */
    create: () => Promise<void>;
}
//# sourceMappingURL=amplify_project_creator.d.ts.map